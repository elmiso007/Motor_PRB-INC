---
phase: 01-painel-change-team-discovery
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - extractor.py
autonomous: true
requirements:
  - PNCT-01
requirements_addressed:
  - PNCT-01
tags:
  - python
  - postgres
  - extractor
  - change_team

must_haves:
  truths:
    - "FonteIncidentes (ABC) declara método abstrato listar_prbs_por_numero(numeros: Sequence[str]) -> List[PRBExistente]"
    - "ServiceNowExtractor implementa listar_prbs_por_numero usando WHERE numero IN (%s, ..., %s) parametrizado (sem SQL injection)"
    - "ServiceNowExtractorMock implementa listar_prbs_por_numero filtrando os PRBs sintéticos do gerador"
    - "Filtro de organização (_filtro_orgs_sni) aplicado em listar_prbs_por_numero — coerência com queries SNow do projeto"
  artifacts:
    - path: "extractor.py"
      provides: "Novo método FonteIncidentes.listar_prbs_por_numero + implementação real (ServiceNowExtractor) + implementação mock"
      contains: "def listar_prbs_por_numero"
      exports: ["FonteIncidentes.listar_prbs_por_numero"]
  key_links:
    - from: "ServiceNowExtractor.listar_prbs_por_numero"
      to: "self._row_para_prb"
      via: "list comprehension reaproveitando mapper único"
      pattern: "self\\._row_para_prb"
    - from: "ServiceNowExtractor.listar_prbs_por_numero"
      to: "_filtro_orgs_sni()"
      via: "concatenação de WHERE clause via f-string + tuple de params"
      pattern: "_filtro_orgs_sni"
---

<objective>
Adicionar à classe abstrata `FonteIncidentes` (ABC) e às duas implementações
(`ServiceNowExtractor` real + `ServiceNowExtractorMock`) o método novo
`listar_prbs_por_numero(numeros: Sequence[str]) -> List[PRBExistente]`. Este
método é a primitiva que o change_team.py (Plan 04) usa para buscar os PRBs da
Change Team direto no SNow sem janela temporal (per D-03).

Implementa: D-03 (query separada SEM janela), reuso de `_row_para_prb` + filtro
de organização (anti-pattern: NÃO duplicar parser).

Purpose: change_team.py precisa de uma capacidade que ainda não existe — buscar
um conjunto arbitrário de PRBs por número exato. Esta plan adiciona essa
capacidade respeitando a arquitetura ABC do extractor (FonteIncidentes é
abstract, implementada por SNow real + Mock). Sem esta plan, change_team.py não
tem como ler do banco.

Output:
- 1 novo método abstrato em FonteIncidentes
- 1 nova implementação real em ServiceNowExtractor (~10-15 linhas)
- 1 nova implementação mock em ServiceNowExtractorMock (~5 linhas)
- todas com docstring no padrão do arquivo
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-painel-change-team-discovery/01-PATTERNS.md
@.planning/phases/01-painel-change-team-discovery/01-RESEARCH.md
@.planning/phases/01-painel-change-team-discovery/01-02-PLAN.md

# Source files referenciados (NÃO depende de Plan 01-01 que toca SQL — paralelo dentro de Wave 2)
@extractor.py
</context>

<artifacts_this_phase_produces>
- **Método ABC** `FonteIncidentes.listar_prbs_por_numero(numeros: Sequence[str]) -> List[PRBExistente]`
- **Implementação real** `ServiceNowExtractor.listar_prbs_por_numero` — query parametrizada
  `WHERE numero IN (%s, ...)` contra `lwsa.service_now_problemas` + filtro de organização
- **Implementação mock** `ServiceNowExtractorMock.listar_prbs_por_numero` — filtro de PRBs
  sintéticos por set de números

Consumidos por:
- Plan 01-04 (change_team.py): `fonte_inc.listar_prbs_por_numero(numeros_ativos)`
- Plan 01-05 (testes): Fake fonte em test_change_team.py implementa este método
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| change_team.py → ServiceNowExtractor | passa lista de strings (PRBs) lida do banco master |
| ServiceNowExtractor → Postgres lwsa.service_now_problemas | query parametrizada com IN (%s, ...) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | SQL injection via lista de numeros | mitigate | Usar EXATAMENTE `placeholders = ",".join(["%s"] * len(numeros))` + `tuple(numeros)` em params. ANTI-pattern proibido: `f"WHERE numero IN ({','.join(numeros)})"` (SQL injection). Padrão verificado em extractor.py:540, 547, 568. |
| T-03-02 | Information Disclosure | descricao do PRB retornada sem filtro de organização | mitigate | Aplicar `_filtro_orgs_sni()` (linha 240-251 do extractor.py atual) — restringe a organizações ativas em config.ORGANIZACOES_ATIVAS. Coerência com listar_prbs_abertos/listar_prbs_para_validacao. |
| T-03-03 | Denial of Service | numeros com 10000+ entradas estoura placeholders | accept | Lista master é controlada (~88 entradas hoje, soft cap definido por D-01). Caso futuro: PostgreSQL aceita >32K placeholders; Postgres 9.2 sem patches conhecidos. Caso degrade, plan futura adiciona batching. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar método abstrato listar_prbs_por_numero em FonteIncidentes (ABC)</name>
  <files>extractor.py</files>
  <read_first>
    - extractor.py — linhas 1-130 (ABCs FonteIncidentes/FonteChamados + imports/typing)
    - extractor.py — linhas 95-130 (verificar a localização exata de @abstractmethod existentes na ABC FonteIncidentes para inserir o novo após eles, antes da próxima classe)
    - .planning/phases/01-painel-change-team-discovery/01-PATTERNS.md (seção "extractor.py (EDIT)" subsec 1 — linhas 236-253, contém docstring + assinatura exata)
  </read_first>
  <action>
    Adicionar método abstrato `listar_prbs_por_numero` na ABC FonteIncidentes
    (classe definida em torno das linhas 95-130 do extractor.py atual; o método
    deve ser adicionado após os outros @abstractmethod da mesma classe, antes do
    final da classe FonteIncidentes — ou seja, antes da classe FonteChamados).

    Conferir imports: `Sequence` já está em `from typing import ... Sequence` nas
    linhas 19-21 (verificado em PATTERNS.md). Se faltar, adicionar.

    Conferir imports: `PRBExistente` já é o tipo usado pelos outros métodos
    similares (`listar_prbs_abertos`, `listar_prbs_para_validacao`). Não adicionar
    import novo.

    Assinatura + docstring exata (per PATTERNS.md linhas 244-252):

    ```python
    @abstractmethod
    def listar_prbs_por_numero(self, numeros: Sequence[str]) -> List[PRBExistente]:
        \"\"\"PRBs por número exato (sem janela temporal — D-03 Phase 1 Change Team).

        Aceita qualquer status. Devolve um PRBExistente por número encontrado
        no SNow. Números fornecidos que não baterem no SNow são silenciosamente
        omitidos — chamador deve comparar input vs output para detectar misses.
        \"\"\"
        ...
    ```

    Restrições críticas:
    - NÃO alterar nenhum outro @abstractmethod existente — preservar todos os
      métodos atuais da ABC (CON-012 LOCKED dependente).
    - Tipo de retorno é `List[PRBExistente]` (não `Sequence` — coerência com
      os outros métodos abstratos da mesma ABC).
    - Tipo de entrada é `Sequence[str]` (lista, tupla ou outra sequência — flexível
      pra change_team.py passar set->list).
    - O corpo do método é `...` (ellipsis) — convenção idiomática para @abstractmethod.
  </action>
  <verify>
    <automated>
      python -c "from abc import ABC; from extractor import FonteIncidentes; assert hasattr(FonteIncidentes, 'listar_prbs_por_numero'), 'metodo abstrato ausente'; assert getattr(FonteIncidentes.listar_prbs_por_numero, '__isabstractmethod__', False), 'metodo nao marcado como abstract'; print('FonteIncidentes ABC OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def listar_prbs_por_numero" extractor.py` retorna >= 1 após esta task (>= 3 após Tasks 2+3).
    - Behavior: tentar instanciar uma subclasse de FonteIncidentes que NÃO implementa listar_prbs_por_numero deve levantar TypeError ("abstract method").
    - Constraint: `grep -c "@abstractmethod" extractor.py` aumentou exatamente em 1 vs estado inicial.
    - Constraint: assinatura dos outros métodos abstratos NÃO mudou (verificável via grep dos nomes).
  </acceptance_criteria>
  <done>
    FonteIncidentes (ABC) tem o novo método abstrato listar_prbs_por_numero com
    docstring explicando D-03 e contrato (números não encontrados omitidos
    silenciosamente).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implementar listar_prbs_por_numero em ServiceNowExtractor (real)</name>
  <files>extractor.py</files>
  <read_first>
    - extractor.py linhas 424-603 (classe ServiceNowExtractor completa — especialmente listar_prbs_para_validacao em 553-578 que é o analog primário)
    - extractor.py linhas 240-264 (`_filtro_orgs_sni` — função helper)
    - extractor.py linhas 461-478 (`_row_para_prb` — mapper)
    - .planning/phases/01-painel-change-team-discovery/01-PATTERNS.md (seção "extractor.py (EDIT)" subsec 2 — linhas 254-285, implementação exata)
  </read_first>
  <action>
    Adicionar método `listar_prbs_por_numero` à classe `ServiceNowExtractor`
    (classe definida em torno da linha 424; o método deve ser adicionado após
    `listar_prbs_para_validacao` em ~linha 578, mantendo agrupamento lógico).

    Implementação exata (per PATTERNS.md linhas 263-277, copiando o idioma de
    listar_prbs_para_validacao):

    ```python
    def listar_prbs_por_numero(self, numeros: Sequence[str]) -> List[PRBExistente]:
        \"\"\"Implementação real — sem janela, sem filtro de status (D-03).

        Aplica filtro de organização (_filtro_orgs_sni) para coerência com o
        resto do extractor. Edge case: lista vazia → retorna [].
        Não levanta exception em PRBs ausentes no SNow — silenciosamente omite.
        \"\"\"
        if not numeros:
            return []
        placeholders = ",".join(["%s"] * len(numeros))
        filtro_org, params_org = _filtro_orgs_sni()
        sql = f\"\"\"
            SELECT numero, descricao_curta, descricao, produto, servidor,
                   prioridade, status, solucao_alternativa, categoria, subcategoria,
                   grupo_designado, atualizacoes, data_abertura, data_encerrado
            FROM {config.SCHEMA_BANCO}.{config.TABELA_PROBLEMAS}
            WHERE numero IN ({placeholders})
              {filtro_org}
        \"\"\"
        rows = self._query(sql, tuple(numeros) + params_org)
        return [self._row_para_prb(r) for r in rows]
    ```

    Pontos críticos (verificados no analog listar_prbs_para_validacao):
    1. Early return em lista vazia (`if not numeros: return []`) — evita SQL
       sintaticamente inválido `WHERE numero IN ()`.
    2. `placeholders = ",".join(["%s"] * len(numeros))` — único jeito SEGURO
       de gerar N placeholders. NUNCA fazer `','.join(numeros)` (SQL injection
       direta — proibido em PATTERNS §Shared Patterns "Placeholders parametrizados").
    3. Tabela referenciada via `{config.SCHEMA_BANCO}.{config.TABELA_PROBLEMAS}`
       (a tabela do SNow, não a master Change Team — D-03 é sobre buscar PRBs
       no SNow, não na master).
    4. `_filtro_orgs_sni()` retorna tupla `(clause_string, params_tuple)` —
       params concatenados com `tuple(numeros) + params_org` (ordem importa
       porque placeholders são posicionais).
    5. Mapper `self._row_para_prb(r)` para CADA row — não reimplementar
       parsing de datetime/prioridade.
    6. Lista de SELECT colunas EXATAMENTE igual à de listar_prbs_para_validacao
       (linhas 565-567 do estado atual) — _row_para_prb espera essas keys
       em row.get(...).

    Restrições críticas:
    - NÃO usar f-string interpolando `numeros` diretamente (SQL injection).
    - NÃO chamar `conn.execute` diretamente — usar `self._query` (linhas 480-487,
      já encapsula try/except + cursor handling).
    - NÃO inventar colunas SELECT diferentes — paridade total com analog.
    - NÃO remover ou alterar `listar_prbs_para_validacao` (CON-012 LOCKED dependente).
  </action>
  <verify>
    <automated>
      python -c "from extractor import ServiceNowExtractor; assert hasattr(ServiceNowExtractor, 'listar_prbs_por_numero'), 'metodo real ausente'; import inspect; src = inspect.getsource(ServiceNowExtractor.listar_prbs_por_numero); assert 'placeholders' in src and '%s' in src, 'nao usa placeholders parametrizados'; assert 'self._row_para_prb' in src, 'nao reusa _row_para_prb'; assert '_filtro_orgs_sni' in src, 'nao aplica filtro de organizacao'; assert \"','.join(numeros)\" not in src and ', '.join(numeros)\" not in src, 'SQL injection detectada — string concatenation com numeros'; print('ServiceNowExtractor.listar_prbs_por_numero OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "def listar_prbs_por_numero" extractor.py` mostra ao menos uma definição dentro da classe ServiceNowExtractor (não só na ABC).
    - Source: assertiva NEGATIVA — `grep -E "','\.join\(numeros\)" extractor.py` retorna 0 matches (zero SQL injection).
    - Source: `grep -c "_row_para_prb" extractor.py` aumenta em pelo menos 1 (a nova chamada).
    - Behavior: instanciar ServiceNowExtractor e chamar `extractor.listar_prbs_por_numero([])` retorna `[]` sem tocar no banco (early return).
    - Behavior (smoke contra banco — opcional manual): chamar `listar_prbs_por_numero(['PRB0050001'])` em ambiente de teste retorna lista com 0 ou 1 elemento (não levanta exception).
  </acceptance_criteria>
  <done>
    ServiceNowExtractor tem o método novo implementado seguindo padrão exato de
    listar_prbs_para_validacao (placeholders parametrizados + _filtro_orgs_sni +
    _row_para_prb + early return em lista vazia).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implementar listar_prbs_por_numero em ServiceNowExtractorMock</name>
  <files>extractor.py</files>
  <read_first>
    - extractor.py linhas 1475-1594 (ServiceNowExtractorMock completo — especialmente o mock de listar_prbs_para_validacao em 1494-1503 que é o analog)
    - .planning/phases/01-painel-change-team-discovery/01-PATTERNS.md (seção "extractor.py (EDIT)" subsec 3 — linhas 286-299, implementação mock exata)
  </read_first>
  <action>
    Adicionar implementação mock à classe `ServiceNowExtractorMock` (classe em
    torno da linha 1475 do extractor.py atual). Localização: após linha 1503
    (após o mock de listar_prbs_para_validacao), antes do próximo método mock
    ou final da classe.

    Implementação exata (per PATTERNS.md linhas 292-299):

    ```python
    def listar_prbs_por_numero(self, numeros: Sequence[str]) -> List[PRBExistente]:
        \"\"\"Mock: pega PRBs sintéticos cujo numero está na lista solicitada.

        Não filtra por status; espelha o comportamento da implementação real
        (D-03 — sem janela). Edge case: lista vazia → retorna [].
        \"\"\"
        if not numeros:
            return []
        alvos = set(numeros)
        todos = self._gerador.gerar_prbs() + self._gerador.gerar_prbs_para_validacao()
        return [p for p in todos if p.prb_id in alvos]
    ```

    Pontos críticos (verificados no analog em linha 1494-1503):
    1. `set(numeros)` para lookup O(1) — analog usa mesma técnica.
    2. Combina `_gerador.gerar_prbs()` (PRBs ativos) + `_gerador.gerar_prbs_para_validacao()`
       (PRBs encerrados na janela) — coerente com "sem janela" porque concatena
       ambas as fontes mock.
    3. Atributo `_gerador` já existe no ServiceNowExtractorMock — não criar atributo novo.
    4. Filtro via list comprehension — não usa nenhuma rede.
    5. Early return em lista vazia.

    Restrições críticas:
    - NÃO inventar novos PRBs aqui — usar APENAS o que `_gerador` já produz.
    - NÃO chamar APIs externas (mock é mock).
    - Coerência mock↔real: ambos retornam `List[PRBExistente]` com mesmos campos.
  </action>
  <verify>
    <automated>
      python -c "import os; os.environ['USAR_MOCKS']='true'; from extractor import criar_fonte_incidentes; fonte = criar_fonte_incidentes(); rows = fonte.listar_prbs_por_numero([]); assert rows == [], 'lista vazia deveria retornar []'; rows2 = fonte.listar_prbs_por_numero(['PRB_QUE_NAO_EXISTE_999']); assert rows2 == [], 'numero inexistente deveria retornar []'; print('ServiceNowExtractorMock.listar_prbs_por_numero OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `python -c "import os; os.environ['USAR_MOCKS']='true'; from extractor import criar_fonte_incidentes; print(criar_fonte_incidentes().listar_prbs_por_numero([]))"` imprime `[]`.
    - Behavior: chamar com lista inteiramente desconhecida retorna `[]`.
    - Behavior: chamar com pelo menos um PRB que `_gerador.gerar_prbs()` produz retorna pelo menos 1 PRBExistente.
    - Source: `grep -c "def listar_prbs_por_numero" extractor.py` retorna >= 3 (ABC + Real + Mock).
    - Constraint: o método mock listar_prbs_para_validacao (linhas 1494-1503) NÃO foi alterado.
  </acceptance_criteria>
  <done>
    ServiceNowExtractorMock tem o método mock implementado, retornando subset
    sintético filtrado por número. Comportamento espelha implementação real
    (lista vazia → [], número desconhecido → omitido). ABC + Real + Mock todos
    coerentes.
  </done>
</task>

</tasks>

<verification>
- `python -c "from extractor import FonteIncidentes, ServiceNowExtractor; assert FonteIncidentes.listar_prbs_por_numero.__isabstractmethod__"` funciona.
- Modo mocks: rodar `python -c "import os; os.environ['USAR_MOCKS']='true'; from extractor import criar_fonte_incidentes; print(len(criar_fonte_incidentes().listar_prbs_por_numero(['PRB_DUMMY'])))"` retorna 0 sem erro.
- Nenhum método pré-existente da ABC, ServiceNowExtractor ou ServiceNowExtractorMock foi alterado.
- Padrão de SQL parametrizado seguido (zero string concatenation com input).
</verification>

<success_criteria>
- FonteIncidentes ABC tem `listar_prbs_por_numero` declarado.
- ServiceNowExtractor tem implementação real com placeholders %s + _filtro_orgs_sni + _row_para_prb.
- ServiceNowExtractorMock tem implementação mock filtrando sintéticos.
- Plan 04 (change_team.py) pode invocar `fonte_inc.listar_prbs_por_numero(numeros_ativos)`.
</success_criteria>

<output>
Criar `.planning/phases/01-painel-change-team-discovery/01-03-SUMMARY.md` documentando
a nova capability da ABC e referenciando o teste integration que Plan 05 vai criar
para cobrir o fluxo end-to-end.
</output>
