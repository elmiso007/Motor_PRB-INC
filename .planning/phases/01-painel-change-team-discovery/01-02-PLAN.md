---
phase: 01-painel-change-team-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - models.py
  - config.py
autonomous: true
requirements:
  - PNCT-01
requirements_addressed:
  - PNCT-01
tags:
  - python
  - dataclass
  - config
  - change_team

must_haves:
  truths:
    - "Dataclass PainelChangeTeamRow existe em models.py com todos os campos de D-05 + D-06 + auditoria"
    - "Constante TABELA_CHANGE_TEAM = 'motor_change_team' existe em config.py"
    - "Constante TABELA_CHANGE_TEAM_PAINEL = 'motor_change_team_painel' existe em config.py"
    - "Toggle CHANGE_TEAM_HABILITADO existe em config.py com default true e leitura via env var (case-insensitive)"
  artifacts:
    - path: "models.py"
      provides: "Dataclass PainelChangeTeamRow"
      exports: ["PainelChangeTeamRow"]
      contains: "@dataclass\\nclass PainelChangeTeamRow"
    - path: "config.py"
      provides: "Constantes de tabela + toggle env var Change Team"
      contains: "TABELA_CHANGE_TEAM"
  key_links:
    - from: "models.PainelChangeTeamRow"
      to: "sql lwsa.motor_change_team_painel columns"
      via: "paridade 1:1 dos 16 campos (9 D-05 + 7 D-06) + snapshot_em"
      pattern: "prb_id|descricao_curta|veredicto|delta_chamados_pct"
    - from: "config.CHANGE_TEAM_HABILITADO"
      to: "validar_entregas.executar_validacao"
      via: "if config.CHANGE_TEAM_HABILITADO: bloco try/except (per D-02, Plan 05)"
      pattern: "CHANGE_TEAM_HABILITADO"
---

<objective>
Criar a camada Python de domínio (models) e configuração (config) que serão
consumidas pelos demais módulos desta phase. Esta plan é Wave 1 — sem dependências,
paralelizável com Plan 01 (SQL DDL). Não toca em código de produção: somente
adições aditivas ao models.py e config.py.

Implementa: D-04 (campos do snapshot), D-05 (9 campos PRBs abertos), D-06 (7
campos pós-resolução), DEC-012 (configurabilidade externa total — zero números
mágicos).

Purpose: Define os contratos de dados (dataclass) e parâmetros operacionais
(env var + nomes de tabela) que change_team.py (Plan 04), notifier_db.py (Plan 04),
extractor.py (Plan 03) e validar_entregas.py (Plan 05) consomem. Sem esta plan,
ninguém downstream pode importar PainelChangeTeamRow nem referenciar
config.TABELA_CHANGE_TEAM_PAINEL.

Output:
- novo dataclass PainelChangeTeamRow em models.py (16 campos + 1 auditoria)
- 2 novas constantes (TABELA_CHANGE_TEAM, TABELA_CHANGE_TEAM_PAINEL) em config.py
- 1 novo toggle (CHANGE_TEAM_HABILITADO) em config.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-painel-change-team-discovery/01-CONTEXT.md
@.planning/phases/01-painel-change-team-discovery/01-PATTERNS.md

# Source files que serão modificados:
@models.py
@config.py
</context>

<artifacts_this_phase_produces>
- **Classe** `models.PainelChangeTeamRow` — dataclass com 16 campos + 1 auditoria.
  Campos obrigatórios (D-05 — sempre populados): prb_id, descricao_curta, produto,
  servidor, status_snow, prioridade_atual, grupo_designado.
  Campos opcionais D-05: dias_em_aberto (Optional[int]), ultima_atualizacao (Optional[datetime]).
  Campos opcionais D-06 (somente PRBs resolvidos): veredicto, data_resolucao,
  dias_pos_resolucao, qtd_incs_pos_resolucao, qtd_incs_pre_resolucao,
  delta_chamados_pct, qtd_prbs_novos_pos_resolucao.
  Campo auditoria: snapshot_em (Optional[datetime]).

- **Constantes** `config.TABELA_CHANGE_TEAM = "motor_change_team"` e
  `config.TABELA_CHANGE_TEAM_PAINEL = "motor_change_team_painel"` (analog: linhas 145-147 — TABELA_PROBLEMAS).

- **Toggle** `config.CHANGE_TEAM_HABILITADO: bool` lido de env var
  `CHANGE_TEAM_HABILITADO` (default "true", case-insensitive — analog: linhas 367-372).

Símbolos consumidos por outras plans:
- Plan 01-03 (extractor): config.TABELA_CHANGE_TEAM em SELECT de master.
- Plan 01-04 (change_team.py): PainelChangeTeamRow como tipo de retorno; config.TABELA_CHANGE_TEAM + config.TABELA_CHANGE_TEAM_PAINEL.
- Plan 01-04 (notifier_db.py): PainelChangeTeamRow na assinatura de persistir_painel_change_team.
- Plan 01-05 (validar_entregas.py): config.CHANGE_TEAM_HABILITADO em `if`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var → config.CHANGE_TEAM_HABILITADO | string externa (PowerShell `$env:`) controla toggle de pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | env var CHANGE_TEAM_HABILITADO | mitigate | Default seguro `"true"` (feature ligada); comparação case-insensitive `.lower() == "true"` — mesmo padrão idiomático que USAR_MOCKS / PERSISTIR_NO_BANCO (analog config.py linhas 367-372). Nada de boolean nativo (env vars são sempre string). |
| T-02-02 | Information Disclosure | PainelChangeTeamRow.descricao_curta em logs | accept | Mesmo nível de risco que ValidacaoEntrega já persistida; audiência interna (Superset corporativo). |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar dataclass PainelChangeTeamRow em models.py</name>
  <files>models.py</files>
  <read_first>
    - models.py (estado atual completo — 242 linhas)
    - .planning/phases/01-painel-change-team-discovery/01-PATTERNS.md (seção "models.py (EDIT)" — linhas 303-358, mostra a dataclass alvo + ordenação de campos)
    - .planning/phases/01-painel-change-team-discovery/01-CONTEXT.md (D-05 e D-06 — colunas exatas exigidas)
  </read_first>
  <action>
    Adicionar nova dataclass `PainelChangeTeamRow` em models.py imediatamente
    após a classe `ValidacaoEntrega` (a inserção vai logo após a última linha
    do dataclass ValidacaoEntrega — o PATTERNS.md indica posicionamento após
    linha 217 do arquivo atual).

    Imports: nenhum novo import necessário — `dataclass`, `field`, `datetime`,
    `Optional` já estão presentes nas linhas 8-11 (verificar e confirmar).

    Estrutura exata da dataclass (per D-05 + D-06 + PATTERNS.md):

    ```
    @dataclass
    class PainelChangeTeamRow:
        \"\"\"Uma linha do painel Change Team materializado em lwsa.motor_change_team_painel.

        Para PRBs abertos (D-05 — Phase 1 CONTEXT): preenche apenas as colunas até
        `ultima_atualizacao` e deixa os campos de veredicto/sinais pós-resolução
        como None/0.

        Para PRBs resolvidos (D-06): adiciona veredicto + sinais derivados de
        `validador_entrega._avaliar_prb` (reaproveitamento, sem duplicação).
        \"\"\"
        # --- Identificação + estado atual (D-05, todos exceto dias_em_aberto/ultima_atualizacao são obrigatórios) ---
        prb_id: str
        descricao_curta: str
        produto: str
        servidor: str
        status_snow: str
        prioridade_atual: str
        grupo_designado: str
        dias_em_aberto: Optional[int] = None
        ultima_atualizacao: Optional[datetime] = None
        # --- Acompanhamento pós-resolução (D-06) — NULL/0 para PRBs abertos ---
        veredicto: Optional[str] = None
        data_resolucao: Optional[datetime] = None
        dias_pos_resolucao: Optional[int] = None
        qtd_incs_pos_resolucao: int = 0
        qtd_incs_pre_resolucao: int = 0
        delta_chamados_pct: float = 0.0
        qtd_prbs_novos_pos_resolucao: int = 0
        # --- Auditoria ---
        snapshot_em: Optional[datetime] = None
    ```

    Restrições críticas (verificar contra PATTERNS.md):
    - Ordem dos campos NÃO arbitrária — campos obrigatórios primeiro, depois
      opcionais com defaults (regra do @dataclass do Python: campos sem default
      devem vir antes dos com default).
    - `delta_chamados_pct: float = 0.0` (NÃO `Optional[float]`) — espelha o
      comportamento de `_avaliar_prb` que sempre retorna float (0.0 se sem dados).
    - `qtd_*` são `int = 0` (NÃO Optional) — espelha `_avaliar_prb`.
    - `dias_em_aberto: Optional[int] = None` — Pitfall 6 do RESEARCH: PRBs sem
      `aberto_em` viram NULL em vez de TypeError.
    - Docstring completa explicando D-05 vs D-06 (verificável via grep).

    Não estender ValidacaoEntrega (decisão registrada em PATTERNS.md "Por que NÃO
    estender" — quebraria Single Responsibility e forçaria ALTER TABLE em
    motor_validacao_entrega que é CON-012 LOCKED).

    Não alterar nenhuma outra classe existente.
  </action>
  <verify>
    <automated>
      python -c "from models import PainelChangeTeamRow; r = PainelChangeTeamRow(prb_id='PRB0050001', descricao_curta='teste', produto='SVA', servidor='srv01', status_snow='Aberto', prioridade_atual='P2', grupo_designado='Change Team'); assert r.veredicto is None and r.qtd_incs_pos_resolucao == 0 and r.delta_chamados_pct == 0.0 and r.dias_em_aberto is None, f'defaults D-06 errados: {r}'; print('PainelChangeTeamRow OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `python -c "from models import PainelChangeTeamRow"` não levanta exception (classe é importável).
    - Behavior: instanciar `PainelChangeTeamRow(prb_id='X', descricao_curta='Y', produto='', servidor='', status_snow='', prioridade_atual='', grupo_designado='')` funciona (7 campos posicionais obrigatórios — D-05 menos os dois Optional).
    - Behavior: campos D-06 têm defaults adequados (None para Optional[...], 0 para int, 0.0 para float).
    - Source: `grep -c "@dataclass" models.py` aumenta em 1 vs estado inicial.
    - Constraint: dataclass ValidacaoEntrega (linhas 154-217 do estado atual) NÃO foi alterada.
  </acceptance_criteria>
  <done>
    PainelChangeTeamRow existe em models.py, importável, espelha 1:1 as colunas
    da tabela lwsa.motor_change_team_painel criada em Plan 01, e nenhum dataclass
    existente foi tocado.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar constantes de tabela + toggle env var em config.py</name>
  <files>config.py</files>
  <read_first>
    - config.py (estado atual — focar nas linhas 145-147 SCHEMA_BANCO/TABELA_INCIDENTES/TABELA_PROBLEMAS e linhas 367-389 bloco "Modo de operação")
    - .planning/phases/01-painel-change-team-discovery/01-PATTERNS.md (seção "config.py (EDIT)" — linhas 458-492, mostra idiomas exatos)
  </read_first>
  <action>
    Adicionar 3 itens em config.py seguindo os padrões idiomáticos do arquivo.

    Item 1 — Constantes de tabela (localização: após linha 179 STATUS_PRB_ENCERRADOS,
    antes da seção "Thresholds do ValidadorEntrega"; ou mesmo grupo de TABELA_PROBLEMAS
    em linhas 145-147 — qualquer localização sensata é aceitável desde que seja
    próximo de outras constantes TABELA_*):

    ```python
    # Tabelas do motor Change Team (Phase 1 do painel — 2026-06-05).
    TABELA_CHANGE_TEAM = "motor_change_team"               # master list soft-deleted (D-01)
    TABELA_CHANGE_TEAM_PAINEL = "motor_change_team_painel"  # snapshot materializado (D-04)
    ```

    Idioma copiado de `TABELA_INCIDENTES = "service_now_incidentes"` (linha 146) e
    `TABELA_PROBLEMAS = "service_now_problemas"` (linha 147) — sem anotação de tipo
    (string literal), comentário inline explicando D-01/D-04.

    Item 2 — Toggle env-var (localização: no bloco "Modo de operação" linhas 367-389,
    após PERSISTIR_NO_BANCO ou similar):

    ```python
    # Habilita/desabilita o snapshot do painel Change Team dentro do ciclo do
    # ValidadorEntrega. Default = "true" (feature ligada em produção). Para
    # desligar temporariamente (ex.: investigação de issue), no PowerShell:
    #   $env:CHANGE_TEAM_HABILITADO = "false"
    CHANGE_TEAM_HABILITADO: bool = (
        os.environ.get("CHANGE_TEAM_HABILITADO", "true").lower() == "true"
    )
    ```

    Idioma idêntico ao padrão de USAR_MOCKS (linha 372), PERSISTIR_NO_BANCO
    (verificado em config.py linhas 367-372):
    - Anotação de tipo `: bool`.
    - Default `"true"` (string, NÃO booleano nativo — env vars são string).
    - `.lower() == "true"` (case-insensitive).
    - Comentário inline explicando como desligar via `$env:` (PowerShell).

    Item 3 — verificar que `import os` já está no topo (linha 1-30). Se não estiver
    sendo usado para env vars (raríssimo dado as linhas 367-372), adicionar.
    Mais provável: já está importado — apenas confirmar.

    Restrições críticas:
    - NÃO alterar nenhuma constante existente — todos os toggles atuais permanecem
      intactos (especialmente USAR_MOCKS, PERSISTIR_NO_BANCO — Phase 0 LOCKED).
    - NÃO usar `bool(os.environ.get(...))` — em Python, qualquer string não-vazia
      vira True, então o default "true" e "false" virariam ambos True (bug clássico).
      Sempre `.lower() == "true"`.
    - NÃO criar constantes intermediárias para fragmentar nomes — usar exatamente
      os identificadores documentados: TABELA_CHANGE_TEAM, TABELA_CHANGE_TEAM_PAINEL,
      CHANGE_TEAM_HABILITADO (consumidos por nome em Plans 03/04/05).
  </action>
  <verify>
    <automated>
      python -c "import config; assert hasattr(config, 'TABELA_CHANGE_TEAM') and config.TABELA_CHANGE_TEAM == 'motor_change_team', 'TABELA_CHANGE_TEAM ausente/errado'; assert hasattr(config, 'TABELA_CHANGE_TEAM_PAINEL') and config.TABELA_CHANGE_TEAM_PAINEL == 'motor_change_team_painel', 'TABELA_CHANGE_TEAM_PAINEL ausente/errado'; assert hasattr(config, 'CHANGE_TEAM_HABILITADO') and isinstance(config.CHANGE_TEAM_HABILITADO, bool), 'CHANGE_TEAM_HABILITADO ausente/nao-bool'; assert config.CHANGE_TEAM_HABILITADO is True, 'default deveria ser True'; print('config Change Team OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `python -c "import config; print(config.TABELA_CHANGE_TEAM, config.TABELA_CHANGE_TEAM_PAINEL, config.CHANGE_TEAM_HABILITADO)"` imprime `motor_change_team motor_change_team_painel True` (default produção).
    - Behavior toggle: rodar com `$env:CHANGE_TEAM_HABILITADO = "false"` (PowerShell), depois `python -c "import config; print(config.CHANGE_TEAM_HABILITADO)"` deve imprimir `False`. (Verificação manual após Task — automated test cobre apenas o default).
    - Source: `grep -c "TABELA_CHANGE_TEAM" config.py` retorna >= 2 (master + painel).
    - Source: `grep -c "CHANGE_TEAM_HABILITADO" config.py` retorna 1 (definição única).
    - Constraint: `grep -c "USAR_MOCKS" config.py` permanece igual ao estado pre-task (USAR_MOCKS intocado).
    - Constraint: `grep -c "PERSISTIR_NO_BANCO" config.py` permanece igual ao estado pre-task.
  </acceptance_criteria>
  <done>
    config.py tem as 3 novas constantes (2 nomes de tabela + 1 toggle env var)
    seguindo idiomas idênticos aos existentes. Defaults seguros (TRUE = feature
    ligada). Constantes existentes intocadas.
  </done>
</task>

</tasks>

<verification>
- `python -c "from models import PainelChangeTeamRow"` funciona.
- `python -c "import config; assert config.TABELA_CHANGE_TEAM == 'motor_change_team'"` funciona.
- Nenhum dataclass ou constante existente foi tocado.
- Tipos espelham fielmente o schema SQL criado em Plan 01.
</verification>

<success_criteria>
- models.py contém o dataclass PainelChangeTeamRow com todos os campos D-05/D-06/auditoria.
- config.py contém TABELA_CHANGE_TEAM, TABELA_CHANGE_TEAM_PAINEL e CHANGE_TEAM_HABILITADO.
- Ambos os arquivos são importáveis sem erro.
- Plans 03, 04 e 05 (downstream) podem fazer `from models import PainelChangeTeamRow`
  e `import config` + usar os novos símbolos.
</success_criteria>

<output>
Criar `.planning/phases/01-painel-change-team-discovery/01-02-SUMMARY.md` listando
os símbolos novos disponíveis para downstream plans.
</output>
