---
phase: 01-painel-change-team-discovery
plan: 05
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-04
files_modified:
  - validar_entregas.py
  - tests/test_change_team.py
autonomous: false
requirements:
  - PNCT-01
requirements_addressed:
  - PNCT-01
tags:
  - python
  - integration
  - tests
  - change_team
  - validator

must_haves:
  truths:
    - "validar_entregas.executar_validacao contém 3º bloco try/except condicional (config.CHANGE_TEAM_HABILITADO) que chama gerar_painel_change_team + persistir_painel_change_team"
    - "Falha no bloco Change Team NÃO altera o resultado de gerar_validacoes_entrega (V3.1 LOCKED — CON-012)"
    - "tests/test_change_team.py cobre: lista vazia, separação abertos/resolvidos, fonte_chamados=None, detecção PRBs faltantes, toggle off, falha de change_team não derruba validador"
    - "Suite completa de testes (existentes + novos) passa: pytest tests/ -v termina com exit code 0"
    - "Smoke test manual: rodar `python validar_entregas.py` em modo mock produz output/validacoes_entrega.json + log de Change Team sem erro"
  artifacts:
    - path: "validar_entregas.py"
      provides: "Integração do snapshot Change Team no entry-point do validador (3º bloco try/except)"
      contains: "if config.CHANGE_TEAM_HABILITADO"
    - path: "tests/test_change_team.py"
      provides: "Suite de testes do módulo change_team"
      min_lines: 100
      contains: "def test_"
  key_links:
    - from: "validar_entregas.executar_validacao"
      to: "change_team.gerar_painel_change_team + notifier_db.persistir_painel_change_team"
      via: "try/except dedicado guardado por config.CHANGE_TEAM_HABILITADO"
      pattern: "if config.CHANGE_TEAM_HABILITADO:"
    - from: "tests/test_change_team.py"
      to: "change_team module"
      via: "monkeypatch de _ler_lista_change_team_ativa + Fake fonte"
      pattern: "monkeypatch.setattr"
---

<objective>
Integrar todo o trabalho das Waves 1 e 2 no entry-point do ValidadorEntrega
(`validar_entregas.executar_validacao`) seguindo o padrão Defense in Depth
(DEC-010), e criar a suite completa de testes que cobre PNCT-01 end-to-end.

Implementa: D-02 (entry-point é o ValidadorEntrega), Pattern 2 do RESEARCH
(Defense in Depth no entry-point — 3º bloco try/except), CON-012 (V3.1 intacto
mesmo com falha do Change Team).

Esta é a plan terminal da Wave 3 — depende de Plan 03 (extractor) e Plan 04
(change_team + notifier_db). Após esta plan, a feature está pronta para ser
rodada via Task Scheduler em produção (6h via Motor-PRB-Validador.bat).

Purpose: até aqui, o subsistema Change Team está coeso em isolamento mas NÃO
conectado ao pipeline. Esta plan faz o wiring final + adiciona regression
protection via tests. Inclui checkpoint humano para Emerson validar o smoke
test antes de ir pra produção.

Output:
- 1 nova região (3º bloco try/except) em validar_entregas.executar_validacao
- 1 novo arquivo tests/test_change_team.py (~120-180 linhas)
- 1 checkpoint humano para validação final
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-painel-change-team-discovery/01-CONTEXT.md
@.planning/phases/01-painel-change-team-discovery/01-PATTERNS.md
@.planning/phases/01-painel-change-team-discovery/01-RESEARCH.md
@.planning/phases/01-painel-change-team-discovery/01-04-PLAN.md

# Source files referenciados:
@validar_entregas.py
@tests/test_validador_entrega.py
@tests/builders.py
@tests/conftest.py
</context>

<artifacts_this_phase_produces>
- **Bloco** em `validar_entregas.executar_validacao` — 3º try/except (após V3.1, antes de gravar_payload_dashboard) condicional em `config.CHANGE_TEAM_HABILITADO`.

- **Arquivo** `tests/test_change_team.py` com no mínimo 6 testes cobrindo Wave 0 Gaps do RESEARCH:
  1. `test_painel_lista_vazia_nao_quebra` — monkeypatch retorna [] → função retorna []
  2. `test_painel_separa_abertos_de_resolvidos` — mix de PRBs com/sem data_resolucao
  3. `test_painel_sem_fonte_chamados` — fonte_chamados=None ainda devolve rows válidas
  4. `test_painel_detecta_prbs_faltantes` — master tem 3 PRBs, SNow só devolve 2 → log.warning
  5. `test_painel_toggle_off` — config.CHANGE_TEAM_HABILITADO=False → executar_validacao não chama gerar_painel_change_team
  6. `test_falha_change_team_nao_derruba_validador` — gerar_painel_change_team levanta exception → execucao.validacoes_entrega intacto + erros.append registrado

- **Helper opcional** `tests/builders.py::make_change_team_row()` — só se simplificar testes; não obrigatório.

Consumidos por: ninguém downstream (Wave 3 é terminal). Produção começa após Plan 06 (documentação) ou direto se Emerson aprovar.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validar_entregas → change_team (lazy import) | falha de import não pode quebrar V3.1 |
| pytest suite → produção | regression suite previne deploy de código quebrado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Change Team derruba veredicto V3.1 (CON-012 violation) | mitigate | Try/except do bloco Change Team é INDEPENDENTE do try/except do gerar_validacoes_entrega. Imports lazy (`from change_team import ...` dentro do `if`). Cobertura via `test_falha_change_team_nao_derruba_validador`. |
| T-05-02 | Repudiation | Falha silenciosa no Change Team | mitigate | `execucao.erros.append(f"change_team: {exc}")` sinaliza falha no JSON dashboard. `log.exception` no log. Operador detecta via Superset (rows desatualizadas) ou via SELECT `MAX(snapshot_em)`. |
| T-05-03 | Tampering | env var CHANGE_TEAM_HABILITADO=False em produção esquecida | mitigate | Default `"true"` (Plan 02). Smoke test do checkpoint valida que produção tem default correto. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar 3º bloco try/except em validar_entregas.executar_validacao</name>
  <files>validar_entregas.py</files>
  <read_first>
    - validar_entregas.py (estado atual completo — 127 linhas)
    - .planning/phases/01-painel-change-team-discovery/01-PATTERNS.md (seção "validar_entregas.py (EDIT)" — linhas 190-228)
    - .planning/phases/01-painel-change-team-discovery/01-RESEARCH.md (Pattern 2 — linhas 297-359)
  </read_first>
  <action>
    Inserir um terceiro bloco try/except dentro de `executar_validacao()`, entre
    o bloco existente do ValidadorEntrega V3.1 (linhas 80-86) e o bloco do JSON
    dashboard (linhas 89-93).

    Localização exata: imediatamente após a linha 86 (`execucao.erros.append(f"validador_entrega: {exc}")`)
    e antes da linha 88 (`# Persistência: JSON sempre ...`).

    Bloco a adicionar (per PATTERNS.md linhas 210-220 + RESEARCH §Pattern 2):

    ```python
    # --- BLOCO NOVO Phase 1: Painel Change Team (Defense in Depth — DEC-010) ----
    # Falha aqui NÃO altera execucao.validacoes_entrega (V3.1 CON-012 LOCKED).
    # Imports lazy dentro do if pra que ImportError não derrube o ciclo todo.
    if config.CHANGE_TEAM_HABILITADO:
        try:
            from change_team import gerar_painel_change_team
            from notifier_db import persistir_painel_change_team
            rows_change_team = gerar_painel_change_team(fonte_inc, fonte_chamados)
            persistir_painel_change_team(rows_change_team)
        except Exception as exc:
            log.exception("Falha no Painel Change Team: %s", exc)
            execucao.erros.append(f"change_team: {exc}")
    # ----------------------------------------------------------------------------
    ```

    Pontos críticos (verificados em PATTERNS.md):
    1. **Toggle** `if config.CHANGE_TEAM_HABILITADO:` — operador pode desligar via env var sem deploy.
    2. **Lazy imports** dentro do `if` — ImportError em change_team/notifier_db NÃO impede o entry-point de iniciar.
    3. **log.exception** (NÃO `log.error`) — preserva traceback completo, idioma idêntico ao bloco V3.1 (linha 85).
    4. **execucao.erros.append(f"change_team: {exc}")** — formato exato linha 86 e 93, prefixo `"change_team:"` em vez de `"validador_entrega:"`.
    5. Ordem: depois do bloco V3.1 (que define `execucao.validacoes_entrega`), antes do JSON dashboard (que precisa ver tudo).
    6. Nome da variável local `rows_change_team` (descritivo) ou apenas `rows` (curto) — qualquer das duas formas é aceitável; preferir `rows_change_team` para clareza.

    Restrições críticas (NÃO violar):
    - NÃO mover o bloco V3.1 existente (linhas 80-86) — CON-012 LOCKED.
    - NÃO ALTERAR a assinatura de `executar_validacao()` (`-> ExecucaoMotor`) — outros chamadores podem depender.
    - NÃO mover `gravar_payload_dashboard` para depois do persistir Change Team — a ordem atual permite que o JSON capture os erros append (incluindo de change_team).
    - NÃO inserir top-level imports (`from change_team import ...` no topo do arquivo) — Lazy import dentro do `if` é o pattern explícito de Defense in Depth.
    - NÃO remover `# Persistência: JSON sempre (rede de segurança), Postgres se habilitado.` (comentário linha 88).

    Após adicionar, validar:
    - Função `executar_validacao()` continua retornando ExecucaoMotor.
    - Chamada do `main()` ainda termina com exit code 0 em ausência de erros.
  </action>
  <verify>
    <automated>
      python -c "import validar_entregas; import inspect; src = inspect.getsource(validar_entregas.executar_validacao); assert 'CHANGE_TEAM_HABILITADO' in src, 'toggle CHANGE_TEAM_HABILITADO ausente'; assert 'from change_team import gerar_painel_change_team' in src, 'lazy import gerar_painel_change_team ausente'; assert 'from notifier_db import persistir_painel_change_team' in src, 'lazy import persistir_painel_change_team ausente'; assert 'change_team: {exc}' in src, 'erros.append com prefix change_team ausente'; assert src.count('try:') >= 3, 'menos de 3 try/except — falta defense in depth'; v31_idx = src.find('gerar_validacoes_entrega'); ct_idx = src.find('gerar_painel_change_team'); assert v31_idx >= 0 and ct_idx >= 0 and v31_idx < ct_idx, 'V3.1 deve vir antes do bloco Change Team'; print('validar_entregas integration OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "try:" validar_entregas.py` aumentou em pelo menos 1 vs estado inicial (3+ blocos try/except no executar_validacao).
    - Source: `grep -c "if config.CHANGE_TEAM_HABILITADO" validar_entregas.py` retorna 1.
    - Source: `grep -c "gerar_painel_change_team" validar_entregas.py` retorna >= 2 (import + chamada).
    - Source: `grep -c "from change_team import" validar_entregas.py` retorna 1 (lazy import — DENTRO do if).
    - Source: `grep -c "from change_team import" validar_entregas.py` no contexto top-level (linhas 1-50) retorna 0 (NÃO é top-level import).
    - Constraint: bloco V3.1 (try com gerar_validacoes_entrega) permanece intocado — `grep -c "gerar_validacoes_entrega" validar_entregas.py` retorna mesmo valor que pré-task.
    - Behavior: `python validar_entregas.py --help` (ou rodar em modo mock) termina sem stacktrace.
  </acceptance_criteria>
  <done>
    validar_entregas.executar_validacao tem 3º bloco try/except condicional em
    config.CHANGE_TEAM_HABILITADO, com lazy imports e log.exception + erros.append.
    V3.1 intacto. main() continua exit 0/1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar tests/test_change_team.py com 6 testes de cobertura PNCT-01</name>
  <files>tests/test_change_team.py</files>
  <read_first>
    - tests/test_validador_entrega.py (padrão de Fake fonte + monkeypatch — analog primário)
    - tests/builders.py (verificar que make_prb existe — se não, criar variante inline)
    - tests/conftest.py (fixtures globais)
    - change_team.py (estado após Plan 04 — assinatura de gerar_painel_change_team + _ler_lista_change_team_ativa)
    - models.py (PainelChangeTeamRow + PRBExistente — campos e defaults)
    - validar_entregas.py (estado após Task 1 — executar_validacao)
    - .planning/phases/01-painel-change-team-discovery/01-PATTERNS.md (seção "tests/test_change_team.py (NEW)" — linhas 154-186)
    - .planning/phases/01-painel-change-team-discovery/01-RESEARCH.md (Phase Requirements → Test Map — linhas 891-901)
  </read_first>
  <behavior>
    - Test 1 (`test_painel_lista_vazia_nao_quebra`): monkeypatch `change_team._ler_lista_change_team_ativa` para devolver [] → `gerar_painel_change_team(fonte_inc=None, fonte_chamados=None)` retorna [] sem chamar `fonte_inc.listar_prbs_por_numero`.
    - Test 2 (`test_painel_separa_abertos_de_resolvidos`): master tem 2 PRBs (1 aberto, 1 resolvido com data_resolucao); fake fonte devolve ambos. Verificar: 2 rows produzidas, a aberta tem veredicto=None, a resolvida tem veredicto em ('REINCIDENCIA', 'ENTREGA_VALIDADA', 'INCONCLUSIVO').
    - Test 3 (`test_painel_sem_fonte_chamados`): master tem 1 PRB resolvido, fake fonte_inc devolve ele, fonte_chamados=None. Verificar: 1 row produzida sem stacktrace, `delta_chamados_pct` = 0.0 ou outro valor numeric válido (não crash).
    - Test 4 (`test_painel_detecta_prbs_faltantes`): master tem 3 PRBs ('PRB1','PRB2','PRB3'); fake fonte só devolve PRB1 e PRB2. Capturar logs (caplog fixture do pytest) e verificar que há `log.warning` com substring "não no SNow" e contendo "PRB3".
    - Test 5 (`test_painel_toggle_off`): set `config.CHANGE_TEAM_HABILITADO = False` (via monkeypatch.setattr). Chamar `validar_entregas.executar_validacao` com mock que conta calls em gerar_painel_change_team. Verificar: counter == 0 (toggle off → bloco pulado).
    - Test 6 (`test_falha_change_team_nao_derruba_validador`): monkeypatch `change_team.gerar_painel_change_team` para levantar `RuntimeError("simulado")`. Chamar `executar_validacao` em modo mock. Verificar: `execucao.validacoes_entrega` é populado (V3.1 funcionou); `"change_team:" in " ".join(execucao.erros)` (erro registrado mas não propagado).
  </behavior>
  <action>
    Criar `tests/test_change_team.py` seguindo o estilo de `tests/test_validador_entrega.py`
    (padrão de Fake fonte + monkeypatch verificado em PATTERNS.md linhas 161-187).

    Estrutura do arquivo:

    1. Topo (imports + Fake fonte):

    ```python
    from __future__ import annotations
    from datetime import datetime, timezone, timedelta
    from typing import List, Dict, Optional, Sequence

    import pytest

    import config
    import change_team
    import validar_entregas
    from models import PRBExistente, PainelChangeTeamRow

    # Reusar make_prb do projeto se existir; senão helper inline.
    try:
        from tests.builders import make_prb
    except ImportError:
        def make_prb(prb_id, status="Em Análise", data_resolucao=None, produto="SVA", servidor="srv01", prioridade_atual="P2", grupo_designado="Change Team", aberto_em=None):
            return PRBExistente(
                prb_id=prb_id,
                descricao_curta=f"Teste {prb_id}",
                descricao="...",
                produto=produto,
                servidor=servidor,
                prioridade_atual=prioridade_atual,
                grupo_designado=grupo_designado,
                status=status,
                aberto_em=aberto_em or (datetime.now(timezone.utc) - timedelta(days=30)),
                data_resolucao=data_resolucao,
            )


    class _FakeFonteCT:
        \"\"\"Fake fonte_inc mínima — implementa apenas o que change_team usa.\"\"\"
        def __init__(self, prbs_por_numero: Dict[str, PRBExistente]):
            self._prbs = prbs_por_numero

        def listar_prbs_por_numero(self, numeros: Sequence[str]) -> List[PRBExistente]:
            return [self._prbs[n] for n in numeros if n in self._prbs]

        # Métodos restantes da ABC: NotImplementedError se _avaliar_prb chamar.
        # Adicionar stubs conforme necessário pra cada teste — preferir setattr(...)
        # em casos específicos pra não inflar o Fake.
    ```

    NOTA: o construtor real de PRBExistente pode diferir do esboço acima — ANTES
    de escrever os testes, ABRIR `models.py` e ler a definição EXATA de PRBExistente
    (incluindo todos os campos obrigatórios e seus tipos). Se algum campo for
    diferente, ajustar o helper make_prb. Estrutura aproximada deve seguir o
    modelo de classes que o projeto já usa em tests/test_validador_entrega.py.

    2. Os 6 testes descritos em `<behavior>` acima. Cada um:
       - Nome `test_*` (descoberta automática do pytest).
       - Docstring de uma linha resumindo a intenção.
       - Setup mínimo (Fake fonte + monkeypatch).
       - Assert claro (idealmente uma asserção principal por teste).
       - Cleanup automático via monkeypatch (não usar setUp/tearDown).

    Padrões idiomáticos (per PATTERNS.md):
    - `monkeypatch.setattr("change_team._ler_lista_change_team_ativa", lambda: [...])` para isolar do banco.
    - `caplog` fixture do pytest para capturar logs (Test 4):
      ```python
      def test_painel_detecta_prbs_faltantes(caplog, monkeypatch):
          monkeypatch.setattr("change_team._ler_lista_change_team_ativa", lambda: ["PRB1","PRB2","PRB3"])
          fonte = _FakeFonteCT({"PRB1": make_prb("PRB1"), "PRB2": make_prb("PRB2")})
          with caplog.at_level("WARNING", logger="change_team"):
              rows = change_team.gerar_painel_change_team(fonte, fonte_chamados=None)
          assert len(rows) == 2
          assert any("PRB3" in msg for msg in caplog.messages)
      ```

    Restrições críticas:
    - NÃO usar `mock.patch` em network APIs (não há rede em modo mock — o projeto
      usa USAR_MOCKS via env var para isso). Usar monkeypatch para módulos.
    - NÃO mockar `validador_entrega._avaliar_prb` em Test 2 — queremos exercitar
      o caminho real para validar que reaproveitamento funciona. Para Test 6,
      mockar `change_team.gerar_painel_change_team` (a função do nosso módulo, não a de validador_entrega).
    - NÃO depender de banco real — todos os testes devem rodar em CI sem Postgres.
    - Tests 5+6 podem precisar de `monkeypatch.setenv("USAR_MOCKS", "true")` ANTES
      de importar criar_fonte_*. Verificar se config carrega env var no import-time.

    Verificar:
    - Cada teste roda isoladamente: `pytest tests/test_change_team.py::test_painel_lista_vazia_nao_quebra -x` passa.
    - Suite completa do arquivo passa: `pytest tests/test_change_team.py -v` mostra 6 PASSED.
    - Suite global ainda passa: `pytest tests/ -v` mantém os 54+ testes existentes verdes.
  </action>
  <verify>
    <automated>
      python -m pytest tests/test_change_team.py -v --tb=short
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: arquivo `tests/test_change_team.py` existe e tem >= 100 linhas.
    - Source: `grep -c "^def test_" tests/test_change_team.py` retorna >= 6 (6 testes mínimos).
    - Behavior: `python -m pytest tests/test_change_team.py -v` termina com exit code 0 e mostra todos os testes como PASSED.
    - Behavior: `python -m pytest tests/ -v` (suite global) termina com exit code 0 — nenhuma regressão em testes pré-existentes.
    - Constraint: tests/test_validador_entrega.py e outros tests/test_*.py NÃO foram alterados.
    - Test coverage: cada um dos 6 nomes listados em `<behavior>` aparece como uma função `test_*` no arquivo.
  </acceptance_criteria>
  <done>
    tests/test_change_team.py cobre PNCT-01 com 6 testes (lista vazia, separação,
    fonte None, faltantes, toggle off, defense in depth). Suite global verde.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano — smoke test em modo mock + validação do log</name>
  <what-built>
    Plans 01..04 + Tasks 1+2 desta plan entregaram:
    - DDL Postgres + seed inicial (Plan 01)
    - Dataclass + config (Plan 02)
    - Extractor com listar_prbs_por_numero (Plan 03)
    - Orquestrador change_team.py + persistência atômica em notifier_db (Plan 04)
    - Integração no entry-point validar_entregas.py + suite de testes (Tasks 1+2 desta plan)

    Antes de ir pra produção, verificar smoke real em modo mock para confirmar:
    - Pipeline completo roda end-to-end sem stacktrace
    - Log mostra mensagens esperadas do Change Team (sem erros)
    - JSON output captura os erros append (se houver)
    - Toggle CHANGE_TEAM_HABILITADO funciona
  </what-built>
  <how-to-verify>
    Smoke test sequencial — em PowerShell, no diretório `Motor PRB-INC`:

    Passo 1 — Smoke test com Change Team HABILITADO + mocks:
    ```
    $env:USAR_MOCKS = "true"
    $env:PERSISTIR_NO_BANCO = "false"
    $env:CHANGE_TEAM_HABILITADO = "true"
    python validar_entregas.py
    ```
    Esperado:
    - Exit code 0.
    - Console mostra `INFO | validador | ...` para V3.1 (mensagem padrão de "Validação concluída").
    - Console mostra `INFO | change_team | Lista Change Team vazia — pulando snapshot.` (em modo mock o banco não existe, então _ler_lista_change_team_ativa devolve [] graciosamente).
    - Sem `Traceback (most recent call last):` em lugar algum.
    - Arquivo `output/validacoes_entrega.json` foi escrito.

    Passo 2 — Toggle OFF (verificar Plan 02 + Task 1):
    ```
    $env:CHANGE_TEAM_HABILITADO = "false"
    python validar_entregas.py
    ```
    Esperado:
    - Exit code 0.
    - Console NÃO mostra mensagens com source `change_team` (bloco pulado).
    - Resto do output igual ao Passo 1 (V3.1 não é afetado).

    Passo 3 — Suite de testes:
    ```
    python -m pytest tests/ -v
    ```
    Esperado:
    - Exit code 0.
    - Todos os 54+ testes (pré-existentes) + 6 novos (test_change_team) passam.
    - Sem warnings/deprecation criticais.

    Passo 4 — Inspeção visual do log do dia:
    ```
    Get-Content "logs/validador-entrega-$(Get-Date -Format 'yyyy-MM-dd').log" -Tail 40
    ```
    Esperado: log file existe, formato UTF-8, sem `ERROR` ou `Falha no Painel Change Team`.

    Passo 5 (OPCIONAL, se Emerson tem acesso ao banco de teste):
    Após rodar DDL (Plan 01) + seed (Plan 01 Task 3) + ciclo real:
    ```
    psql -c "SELECT count(*), max(snapshot_em) FROM lwsa.motor_change_team_painel;"
    ```
    Esperado: count entre 0 e ~88, snapshot_em recente (últimos 6h).

    Emerson confirma com "smoke OK" para encerrar checkpoint, ou descreve issue
    encontrado para correção.
  </how-to-verify>
  <resume-signal>
    Digite "smoke OK" para aprovar e marcar PNCT-01 como Complete, OU descreva
    o issue encontrado (stacktrace, log inesperado, teste falhando) para retornar
    à task de origem (Plan 01-05 Task 1 ou Task 2, ou Plan 04 se for issue de
    persistência).
  </resume-signal>
</task>

</tasks>

<verification>
- 3º bloco try/except presente em executar_validacao, condicional em CHANGE_TEAM_HABILITADO.
- Lazy imports (dentro do if) confirmados — top-level imports de change_team/notifier_db.persistir_painel_change_team NÃO existem.
- Suite de testes em tests/test_change_team.py com 6+ testes, todos verdes.
- Suite global (todos os tests/*.py) verde — zero regressão.
- Smoke manual em modo mock: exit 0, log limpo, JSON gerado, toggle funciona.
- Bloco V3.1 (CON-012 LOCKED) intacto.
</verification>

<success_criteria>
- PNCT-01 atende todos os success criteria do ROADMAP.md (lista canônica em tabela,
  fronteira tecnológica = Superset SQL direto via D-07, cadência 6h via D-02,
  audiência implícita = quem tem Superset).
- Operador consegue rodar `python validar_entregas.py` em produção (mocks=false,
  persistir=true, change_team_habilitado=true) e ver os 88 rows em lwsa.motor_change_team_painel
  após um ciclo.
- Falha do Change Team NÃO altera o output de gerar_validacoes_entrega — CON-012 preservado.
</success_criteria>

<output>
Criar `.planning/phases/01-painel-change-team-discovery/01-05-SUMMARY.md` documentando:
- Integration point exato em validar_entregas.py
- Lista dos 6 testes criados e o que cada um valida
- Resultado do smoke test do checkpoint (PASS ou issue encontrado + resolução)
- Quaisquer follow-ups para Phase 2 (chart Superset manual, monitoramento de duracao_ciclo_ms)
</output>
